#!/usr/bin/env python
from distutils.core import setup

setup(name='ecto_ros',
      version='0.3.0',
      description='Ecto bindings for ROS',
      packages=['ecto_ros'],
      package_dir={'':'python'}
)
