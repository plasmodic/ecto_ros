#!/usr/bin/env python
from distutils.core import setup

setup(name='Ecto ROS',
      version='1.0.0',
      description='Ecto bindings for ROS',
      packages=['ecto_ros'],
      package_dir={'':'python'}
)
